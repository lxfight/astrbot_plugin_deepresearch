{
    "search_engines": {
        "description": "预设的搜索引擎模板列表",
        "type": "list",
        "default": [
            "https://duckduckgo.com/html/?q={query}",
            "https://www.bing.com/search?q={query}",
            "https://www.google.com/search?q={query}&hl=en"
        ],
        "hint": "插件将同时使用列表中的所有引擎进行搜索。{query} 会被替换为搜索词。您可以任意添加或删除。"
    },
    "max_sub_queries": {
        "description": "LLM解析生成的最大子问题数量",
        "type": "int",
        "default": 3
    },
    "max_results_per_query": {
        "description": "每个子问题最多抓取几条搜索结果",
        "type": "int",
        "default": 5
    },
    "max_links_to_process": {
        "description": "在所有搜索结果中，最终选择多少个链接进行内容抓取和分析",
        "type": "int",
        "default": 5
    },
    "request_timeout": {
        "description": "网络请求的超时时间（秒）",
        "type": "int",
        "default": 15
    }
}